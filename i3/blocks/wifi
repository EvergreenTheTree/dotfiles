#!/usr/bin/env bash

case $BLOCK_BUTTON in
  1) termite -e 'nmtui-connect' --name '__nmtui' >/dev/null 2>&1 &;;
  3) termite -e 'nmtui-edit' --name '__nmtui' >/dev/null 2>&1 &;;
esac

TEXT=""
echo "$TEXT"
echo "$TEXT"
